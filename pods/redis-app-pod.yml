apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
    app: demo-voting-app
spec:
  containers:
    - name: redis
      image: redis
      resources:
        limits:
          cpu: 100mi
          memory: 200m
      ports:
        - containerPort: 6379